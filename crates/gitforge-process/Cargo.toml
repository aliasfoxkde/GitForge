[package]
name = "gitforge-process"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "sync", "time", "io-util", "process", "macros", "signal"] }
tracing = "0.1"
libc = "0.2"
thiserror = "2"

[dev-dependencies]
tokio-test = "0.4"
tokio = { version = "1", features = ["rt", "sync", "time", "io-util", "process", "macros"] }
